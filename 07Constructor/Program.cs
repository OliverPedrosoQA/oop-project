using System;

namespace _06Encapsulation
{
    class Program
    {
        static void Main(string[] args)
        {
            Person obj = new Person("Peter");
            // Person obj = new Person(); it's possible to run the constructor with a parameter or without
        }
    }
}